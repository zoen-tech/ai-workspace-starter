<!--
このファイルはテンプレートです。
`{{ }}` をヒアリング結果で置換し、ワークスペース直下に
CLAUDE.md（Claude Code）または AGENTS.md（Codex・Cursor・Antigravity・VS Code等）として保存してください。
該当しないセクション（例：個人利用ならチーム同期）は削除して構いません。
このコメントブロックも生成時に削除してください。
-->

# {{WORKSPACE_NAME}} ワークスペース

## ワークスペースの概要

ここは {{OWNER_NAME}} の業務ワークスペース。{{BUSINESS_SUMMARY（どんな業務領域を含むかを1〜2文で）}}。施策の立案・管理、資料作成、情報管理をこの中で行う。

## 構成ルール

- `repo_` プレフィックスのフォルダ → Gitリポジトリ
- プレフィックスなしのフォルダ → ローカル専用（{{LOCAL_FOLDERS（例：`downloads/` 等、Q4-3の回答）}}）
{{IF_TEAM（チーム利用の場合のみ）: - ワークスペース直下のこのファイルは各メンバーのローカルファイル（Git管理なし）。ブラッシュアップやナレッジの追加があった場合は、workspace-setup リポジトリのテンプレート原本にも内容を反映し、コミット＆プッシュすること}}
{{IF_BOTH_AI（Claude Codeと他ツールを併用する場合のみ）: - `CLAUDE.md` と `AGENTS.md` は同一内容を保つ。片方を更新したら必ずもう片方も更新する}}

## リポジトリ一覧

| フォルダ | 内容 |
|---------|------|
{{REPO_TABLE（Step 3で作成した全リポジトリ＋ローカル専用フォルダの行）}}

## 作業のルール

### 作業開始時の手順

1. リクエスト内容から、**どの業務領域（リポジトリ）で作業するか**を判断する
2. 該当リポジトリ直下のコンテキストファイルを必ず読み込む
3. 依頼内容から必要だと判断したフォルダやファイルも読み込んでから作業を開始する

### 新規プロジェクト作成時の手順

新しいプロジェクト/フォルダを作成する際は、そのプロジェクト用のコンテキストファイルに以下の4項目を記載する。作業前にユーザーへヒアリングし、回答をもとにAIがドラフトを書くこと（AIの想像で埋めない）。可能ならCodex等の別系統のLLMでクロスレビューし、最後にユーザーが確認して仕上げる。

| 項目 | 何を書くか |
|------|-----------|
| ① Why（目的） | このフォルダ/プロジェクトの存在理由 |
| ② Structure（構成） | フォルダの構造と各ディレクトリの役割 |
| ③ Rules（ルール） | AIに守らせたいルール・禁止事項 |
| ④ Workflow（手順) | よくある作業の手順・パイプライン |

空欄の項目がある場合は、その旨を明記して残す（後日追記できる状態にしておく）。

{{IF_TEAM（チーム利用の場合のみ、このセクションを残す）:
### リポジトリの同期（新リポが増えたとき）

ワークスペースに置くリポジトリの一覧は `repo_workspace-setup/scripts/repos.tsv`（マニフェスト＝単一の正）で管理する。

- **新しいGitHubリポジトリを作成・共有したとき**（AIが作成を代行した場合も含む）:
  1. `repo_workspace-setup/scripts/repos.tsv` に `<org/repo><TAB><ローカルパス><TAB><区分>` を1行追記する
  2. `repo_workspace-setup` をコミット＆push
  3. メンバーに「`sync-repos.sh` を実行して」と通知する
- **自分のワークスペースに未cloneのリポを取り込むとき**（ターミナルで実行。フォルダ名は自チームのworkspace-setupリポジトリ名に合わせる）:
  ```bash
  git -C repo_workspace-setup pull
  bash repo_workspace-setup/scripts/sync-repos.sh
  ```
}}

### 基本ルール

- 必ず{{LANGUAGE}}で回答すること
{{IF_GITHUB（GitHub利用時のみ残す）: - **リポジトリ内のファイルを編集する前に、必ず `git pull` で最新の状態にすること**}}
- 機密情報（{{SENSITIVE_EXAMPLES（ユーザーの業務における機密情報の種類。認証情報・顧客の個人情報など）}}）はリポジトリにコミットしない
{{IF_GITHUB（GitHub利用時のみ残す）: - **公開（public）リポジトリで対外ドキュメント（提案書・取引先向け資料等）をWeb配布する場合は、機密度で3区分して置き場を決める**：(A) 誰に読まれても問題ない資料 → 恒久掲載可 / (B) 商談用の短命資料 → 「相手が既に知っている情報＋匿名化した相場水準」のみで構成し、推測不能なランダムURL（相手名を入れない）＋noindexで公開、privateリポ側の台帳に登録して用済み後は削除 / (C) 実名の相見積比較・原価・粗利・個人情報 → 公開リポ禁止（クラウドストレージの特定アカウント共有等で渡す。原本はprivateリポに置く）。ランダムURL・noindex・簡易パスワードは偶然アクセスの防止であってアクセス制御ではない点に注意}}
- 写真・動画などの高容量ファイル（目安50MB超、動画は一律）はリポジトリに入れない。クラウドストレージに置いてローカル同期で運用し、リポジトリ側には目次md（ファイル名・内容・日付・リンクの一覧）を置く。該当フォルダにファイルを追加したら目次もAIが更新する
{{EXTRA_RULES（ヒアリングで出たユーザー独自のルールがあれば箇条書きで追加）}}

### 意思決定ログの記録
セッション中に業務・組織・設計レベルの意思決定が確定したら（方針の決定、選択肢からの採用、価格・体制・施策の変更等）、**その場で該当リポジトリの `docs/decisions/` に記録すること**（都度の確認は不要）。「誰が・いつ・どういう背景で・何を決めたか」が後から追える状態が合格ライン。

- 記録先は「その決定が属する単位」のリポジトリ（事業の決定→事業リポ、組織共通→共通リポ、ワークスペース設計→workspace-setup相当）
- ファイル名: `YYYY-MM-DD-<テーマ>.md`。日付・決定者・背景・検討した選択肢・決定事項・理由・影響を含める
- フォルダを作るだけでは記録は生まれない。**AIが自動で書き込むこのルールが導線**であり、人の手作業に頼らない

### 応答・報告フォーマット

多忙な読み手が短時間で「何が起きて、自分は何をすればいいか」を判断できることを最優先に構成する。

**共通原則：どんな報告も冒頭2行で宣言する**

```
【タグ】結論を一文で
▶あなたのアクション：なし／確認のみ／1〜3から選択／〜の判断
```

**4類型**

| タグ | 場面 | 構成 |
|------|------|------|
| 【完了】 | 作業が終わった | 結論（何をした＋結果）→ 確認ポイント → 補足（読まなくてよい詳細） |
| 【相談】 | 判断・選択が必要 | 結論 → 背景（最大3点）→ 推奨（1つ＋理由1文）→ 選択肢 1/2/3 |
| 【報告】 | 問題・遅延が発生 | 結論（何が起きた）→ 現状/原因 → 対応案（推奨付き）→ 影響範囲 |
| 【共有】 | 情報のみ | 結論のみ3行以内。アクション不要と明示 |

**ルール**

1. 冒頭サマリは10行以内。判断に必要な材料はすべてサマリに入れ、詳細は「補足」以下に隔離する
2. 判断を求めるときは判断項目を質問文で書く（「Aで進めてよいか？」）
3. 選択肢には必ず推奨を1つ付ける。選択肢の記号は数字（1/2/3）を使う
4. 経過・試行錯誤の説明は求められない限り書かない
5. 確認を求めるときは、成果物への直接アクセス手段（URL・ファイルパス等）を必ず添える。「確認してください」だけで場所を示さない

### ファイル/フォルダの移動・リネーム・削除時のチェックリスト

ファイル構造を変更する時は、必ず以下を確認してからコミットすること。旧パスを参照しているものが残ると、リンク切れ・自動化の停止などの障害が起きる。

1. 旧パスを grep で全文検索して参照箇所を洗い出す
2. 特に確認するもの：CI/自動化の設定（`.github/workflows/` 等）・README等のパス記載・Markdown間の相対リンク・スクリプト内のパス
3. コミット前に `git status` で意図しない変更がないか確認する

{{IF_KNOWLEDGE（ナレッジリポジトリを作成した場合のみ、このセクションを残す）:
## ナレッジ

### 作業開始時の参照

{{KNOWLEDGE_AREAS（ナレッジ対象領域）}}に関する作業を開始する際は、`repo_{{KNOWLEDGE_REPO_NAME}}/` 内の関連ファイルを読み、過去のナレッジを踏まえて作業すること。

### フィードバック検出時の蓄積

ユーザーが業務のやり方について以下のようなフィードバックや修正指示を出した場合、**個別具体のルールではなく、その背後にある原理原則を抽出して** `repo_{{KNOWLEDGE_REPO_NAME}}/` の該当ファイルに追記すること。

**検出すべきフィードバックの例：**
- 「こう書いて」「こうして」「こっちのほうがいい」（表現・方法論の指示）
- 「分かりにくい」（表現の改善指示）
- 「〜も考えて」「〜も見て」（視点・網羅性の指摘）
- 「〜は違う」「〜ではなく〜」（概念の修正）

**原則の抽出方法：**
1. 「なぜそうすべきか？」を問い、個別ルールの背後にある理由を言語化する
2. 業種・場面の限定を外し、横展開可能な表現にする
3. 具体例は「背景例」として残す
4. 原則が自信を持って抽出できない場合は、仮説を立てて「こういう原則ですか？」とユーザーに確認する

**蓄積先のファイル：**

| ファイル | 内容 |
|---------|------|
{{KNOWLEDGE_FILE_TABLE（Q4-2の回答で決めた領域別ファイル）}}

**蓄積のルール：**
- 顧客固有の情報（社名・URL等）は含めない
- フィードバックを検出したら、その場で該当ファイルに追記する（確認は不要）
- どのリポジトリで作業していても、ナレッジは常に `repo_{{KNOWLEDGE_REPO_NAME}}/` に書く
- 既存エントリと本質的に同じ原則の場合は、背景例を追記するだけでよい
- スキル（定型作業の手順書）を新規作成・更新するときは、関連するナレッジファイルがあれば「作業前に読む」手順としてスキルに組み込む（ナレッジが自動で効く導線にする）
}}
